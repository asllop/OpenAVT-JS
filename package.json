{
  "name": "openavt-js",
  "version": "0.1.0",
  "description": "OpenAVT for Javascript-based platforms like web browsers, smart TVs, chromecast and similar.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dist-build": "npm run clean && npm run build",
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "python -m SimpleHTTPServer 8000 &"
  },
  "author": "asllop",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.3.5"
  },
  "files": [
    "/dist"
  ]
}
